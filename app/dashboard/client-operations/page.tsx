"use client";
import { useEffect, useState } from "react";
import { auth } from "@/lib/firebase";
export default function ClientOperations() { const [data, setData] = useState<any>(null); const [reply, setReply] = useState<Record<string, string>>({}); const load = async () => { const token = await auth.currentUser?.getIdToken(); const r = await fetch("/api/admin/client-operations", { headers: { Authorization: `Bearer ${token}` } }); if (r.ok) setData(await r.json()); }; useEffect(() => { void load(); }, []); const patch = async (body: any) => { const token = await auth.currentUser?.getIdToken(); await fetch("/api/admin/client-operations", { method: "PATCH", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` }, body: JSON.stringify(body) }); void load(); }; if (!data) return <div className="empty-state">Chargement…</div>; const name = (id: string) => data.dogs.find((dog: any) => dog.id === id)?.nom || "Compagnon"; const owner = (id: string) => data.owners.find((item: any) => item.id === id); return <div className="space-y-8"><header className="page-intro"><div><p className="eyebrow">Espace client</p><h1>Piloter les familles.</h1><p>Demandes, paiements et échanges clients au même endroit.</p></div></header><section className="calm-panel"><p className="eyebrow">Demandes de garde</p><div className="mt-4 space-y-3">{data.requests.map((item: any) => <article key={item.id} className="request-card"><div className="flex-1"><b>{name(item.dogId)}</b><p className="mt-1 text-sm">Du {item.dateDebut} au {item.dateFin} · {item.typePrestation}</p><p className="mt-2 text-sm text-[#68776e]">{item.notes}</p></div><div className="flex gap-2"><button onClick={() => patch({ type: "request", id: item.id, status: "acceptée" })} className="request-action">Accepter</button><button onClick={() => patch({ type: "request", id: item.id, status: "liste_attente" })} className="request-action">Attente</button><button onClick={() => patch({ type: "request", id: item.id, status: "refusée" })} className="request-action">Refuser</button></div></article>)}</div></section><section className="calm-panel"><p className="eyebrow">Paiements</p><div className="mt-4 space-y-3">{data.bookings.map((item: any) => <article key={item.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-[#e0e7df] p-4"><span><b>{name(item.dogId)}</b> · {item.prix || 0} €</span><button onClick={() => patch({ type: "payment", id: item.id, paid: !item.paidAt, paymentMethod: "manuel" })} className="request-action">{item.paidAt ? "Annuler reçu" : "Marquer reçu"}</button></article>)}</div></section><section className="calm-panel"><p className="eyebrow">Messages clients</p><div className="mt-4 space-y-4">{data.messages.filter((item: any) => item.sender === "client").map((item: any) => <article key={item.id} className="rounded-xl bg-[#f2f7f3] p-4"><b>{owner(item.ownerId)?.prenom || "Client"}</b><p className="mt-2 text-sm">{item.text}</p><textarea value={reply[item.ownerId] || ""} onChange={(e) => setReply({ ...reply, [item.ownerId]: e.target.value })} className="calm-control mt-3" placeholder="Répondre…" /><button onClick={() => patch({ type: "message", ownerId: item.ownerId, text: reply[item.ownerId] })} className="calm-primary mt-2">Envoyer la réponse</button></article>)}</div></section></div>; }
