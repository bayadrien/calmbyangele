"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { auth } from "@/lib/firebase";
export default function ClientJournal() { const [entries, setEntries] = useState<any[]>([]); useEffect(() => { const load = async () => { const token = await auth.currentUser?.getIdToken(); const r = await fetch("/api/client/journal", { headers: { Authorization: `Bearer ${token}` } }); if (r.ok) setEntries((await r.json()).entries); }; void load(); }, []); return <main className="public-calm min-h-screen p-5"><header className="mx-auto max-w-3xl"><Link href="/client" className="brand"><span>♧</span><b>CALM <em>by Angèle</em></b></Link></header><section className="mx-auto mt-10 max-w-3xl"><p className="eyebrow">Journal de séjour</p><h1 className="gallery-title">Les nouvelles du jour.</h1><div className="mt-7 space-y-3">{entries.map((x) => <article key={x.id} className="calm-panel"><b>{x.humeur}</b><p className="mt-2 text-sm"><strong>Repas :</strong> {x.repas}</p><p className="mt-1 text-sm"><strong>Activité :</strong> {x.balade}</p><p className="mt-3 text-sm text-[#68776e]">{x.note}</p></article>)}</div>{!entries.length && <p className="empty-state mt-6">Les premières nouvelles arriveront bientôt.</p>}</section></main>; }
