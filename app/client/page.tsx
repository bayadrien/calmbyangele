"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { auth } from "@/lib/firebase";
import { signOut } from "firebase/auth";
type Portal = { owner: any; dogs: any[]; bookings: any[]; contracts: any[]; stayContracts: any[]; photos: any[] };
export default function ClientPortal() { const [data, setData] = useState<Portal | null>(null); const [error, setError] = useState(""); useEffect(() => { const load = async () => { const token = await auth.currentUser?.getIdToken(); if (!token) return setError("CONNECT"); const response = await fetch("/api/client/portal", { headers: { Authorization: `Bearer ${token}` } }); if (!response.ok) return setError(response.status === 404 ? "NO_OWNER" : "CONNECT"); setData(await response.json()); }; const unsub = auth.onAuthStateChanged(() => { void load(); }); return unsub; }, []); if (error === "CONNECT") return <main className="public-calm grid min-h-screen place-items-center p-5"><section className="access-card text-center"><h1>Connectez-vous à votre espace.</h1><Link href="/client/login" className="calm-primary mt-6">Connexion <span>→</span></Link></section></main>; if (error === "NO_OWNER") return <main className="public-calm grid min-h-screen place-items-center p-5"><section className="access-card"><h1>Adresse e-mail non reconnue.</h1><p>Utilisez l’adresse communiquée à CALM ou contactez Angèle pour rattacher votre compte.</p></section></main>; if (!data) return <main className="public-calm grid min-h-screen place-items-center"><div className="loading-mark">♧</div></main>; const dogName = (id: string) => data.dogs.find((dog) => dog.id === id)?.nom || "Votre compagnon"; const pending = [...data.contracts, ...data.stayContracts].filter((contract: any) => contract.statut === "en_attente"); const total = data.bookings.reduce((sum: number, item: any) => sum + Number(item.prix || 0), 0); return <main className="public-calm min-h-screen p-5 sm:p-8"><header className="mx-auto flex max-w-5xl items-center justify-between"><Link href="/" className="brand"><span>♧</span><b>CALM <em>by Angèle</em></b></Link><button onClick={() => signOut(auth)} className="text-sm font-bold text-[#315e4e]">Déconnexion</button></header><section className="mx-auto mt-10 max-w-5xl"><p className="eyebrow">Espace famille</p><h1 className="gallery-title">Bonjour {data.owner.prenom}.</h1><p className="mt-3 text-[#68776e]">Tout ce qui concerne vos compagnons, réuni avec soin.</p><div className="mt-7 grid gap-3 sm:grid-cols-3"><div className="metric-card"><p>Compagnons</p><p>{data.dogs.length}</p></div><div className="metric-card"><p>Séjours</p><p>{data.bookings.length}</p></div><div className="metric-card"><p>Total des séjours</p><p>{total} €</p></div></div><section className="mt-7 grid gap-5 lg:grid-cols-2"><article className="calm-panel"><p className="eyebrow">Mes compagnons</p><h2 className="mt-2 text-xl font-bold">Leur fiche</h2><div className="mt-4 space-y-3">{data.dogs.map((dog) => <div key={dog.id} className="rounded-xl bg-[#f2f7f3] p-3"><b>{dog.nom}</b><p className="mt-1 text-sm text-[#68776e]">{dog.race || "Compagnon adoré"}{dog.alertesSante && ` · Santé : ${dog.alertesSante}`}</p></div>)}</div></article><article className="calm-panel"><p className="eyebrow">Contrats</p><h2 className="mt-2 text-xl font-bold">À suivre</h2><div className="mt-4 space-y-3">{pending.length ? pending.map((contract: any) => <Link key={contract.id} href={contract.token ? (contract.bookingId ? `/contrat-sejour/${contract.token}` : `/contrat/${contract.token}`) : "#"} className="block rounded-xl bg-[#fff4e9] p-3 text-sm font-bold text-[#89533d]">Contrat à signer pour {dogName(contract.dogId)} →</Link>) : <p className="rounded-xl bg-[#f2f7f3] p-3 text-sm text-[#52705f]">Tous vos contrats sont à jour.</p>}</div></article></section><section className="mt-5 calm-panel"><p className="eyebrow">Mes séjours</p><div className="mt-4 space-y-3">{data.bookings.map((booking: any) => <article key={booking.id} className="rounded-xl border border-[#e0e7df] p-4"><b>{dogName(booking.dogId)}</b><p className="mt-1 text-sm text-[#68776e]">Du {booking.dateDebut} au {booking.dateFin} · {booking.prix || 0} € · {booking.paidAt ? "Reçu" : "À régler"}</p></article>)}</div></section></section></main>; }
