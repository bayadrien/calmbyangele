"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { auth } from "@/lib/firebase";
export default function ClientPhotos() { const [photos, setPhotos] = useState<any[]>([]); useEffect(() => { const load = async () => { const token = await auth.currentUser?.getIdToken(); const r = await fetch("/api/client/portal", { headers: { Authorization: `Bearer ${token}` } }); if (r.ok) setPhotos((await r.json()).photos); }; void load(); }, []); return <main className="public-calm min-h-screen p-5"><header className="mx-auto flex max-w-5xl justify-between"><Link href="/client" className="brand"><span>♧</span><b>CALM <em>by Angèle</em></b></Link><Link href="/client" className="marketing-login">Mon espace</Link></header><section className="mx-auto mt-10 max-w-5xl"><p className="eyebrow">Les souvenirs</p><h1 className="gallery-title">Les moments de garde.</h1><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{photos.map((photo) => <article key={photo.id} className="gallery-photo"><img src={photo.imageUrl} alt={photo.caption || "Souvenir de garde"} />{photo.caption && <p>{photo.caption}</p>}</article>)}</div>{!photos.length && <p className="empty-state mt-7">Les premiers souvenirs arriveront ici pendant les séjours.</p>}</section></main>; }
